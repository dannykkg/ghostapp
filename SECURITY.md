# Security

GhostApp 能够调用包管理器并移动用户文件，因此安全边界优先于清理覆盖率。

## 当前边界

- 扫描不要求 root；
- 不读取凭据和会话内容；
- 删除默认是 dry-run；
- 敏感数据需要独立标志；
- 文件移动目标的词法路径和解析后的父目录必须位于当前用户主目录下；
- Documents、Desktop、Downloads、Pictures、Movies、Music 及其子路径不自动移动；
- 系统级文件和 Shell 配置只提示人工检查；
- 中、低可信度关联只提示人工检查；
- 外部命令限制为受信任安装根目录中的内置 Provider 卸载程序和 `/bin/launchctl`；
- 外部卸载命令失败时停止后续关联数据清理；
- 外部卸载返回成功后，验证扫描到的程序路径已经消失；
- 外部命令有超时，输出读取大小受限；
- 删除使用废纸篓，不使用递归永久删除。
- 冻结计划使用 SHA-256 校验内容，并记录目标的 device/inode/符号链接身份；
- 每次真实执行记录事务清单，`undo` 仅恢复 GhostApp 移入废纸篓的文件。

## HTML 报告

`ghostapp report` 只生成本地静态文件，不上传数据，不读取凭据、会话或用户文件正文。报告不使用外部资源、网络请求、数据库或浏览器持久存储；完整扫描 JSON 直接内嵌在 HTML 中。当前用户主目录会替换为 `~`。

报告仍可能包含软件名、版本、主机名、系统路径和配置元数据。对外分享前应先检查其内容。

## 报告问题

请避免在公开 Issue 中粘贴用户名、完整主目录路径、Token、会话正文或完整扫描 JSON。报告路径相关问题时应先脱敏。
